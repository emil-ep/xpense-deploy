apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-script
  namespace: xpense
data:
  init.sql: |
    -- Create mf database if it doesn't exist
    SELECT 'CREATE DATABASE mf'
    WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'mf')\gexec
